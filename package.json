{
  "name": "heroku-build",
  "scripts": {
    "build": "npm install -g yarn && cd client && yarn && yarn run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "yarn run build && yarn run deploy && echo 'Client built!'"
  }
}
